/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  // basePath: "/nsb",
  // assetPrefix: "/nsb/",
  images: {
    unoptimized: true,
  },
    // trailingSlash: true,
};

export default nextConfig;
